{
  // dp's waybar configuration
  //
  // module docs: https://github.com/Alexays/Waybar/wiki


  "include": [
    "~/.config/waybar/custom/clock.json",
    "~/.config/waybar/custom/hardware.json",

    "~/.config/waybar/custom/left.json",
    "~/.config/waybar/custom/center.json",
    "~/.config/waybar/custom/right.json",
  ],

  "layer": "top", // Waybar at top layer
  "position": "top", // Waybar position (top|bottom|left|right)
  "height": 35,
  "margin": "0 0 0 0",
  //"width": 1350, // Waybar width
  // Choose the order of the modules idle_inhibitor



  // Modules configuration


  "idle_inhibitor": {
    "format": "{icon}",
    "format-icons": {
       "activated": "",
        "deactivated": ""
    }
  },

  

  // *** example of scheduled exec: ***

  // "custom/pacman": {
  //   "format": " {}",
  //   "interval": 7200, // every two hours
  //   "exec": "i=$(checkupdates); echo \"$i\" |wc -l; echo \"$i\" |column -t |tr '\n' '\r'", // # of updates and tooltip details
  //   "exec-if": "exit 0", // always run; consider advanced run conditions
  //   "on-click": "alacritty -e sudo pacman -Syu", // update system
  //   "signal": 8
  // },

}
